# Dev-only: target for the /api/* proxy in next.config.ts (see rewrites()).
# Never read in client code, never bundled to the browser — the frontend
# only ever calls same-origin relative paths like /api/auth/signup.
BACKEND_URL=http://127.0.0.1:8000
